[package]
name = "pbt"
version = "2026.0.0"
edition = "2024"
rust-version = "1.94"
publish = false

[dependencies]
raden = { path = ".." }

[dev-dependencies]
proptest = "1"

# Cargo は tests 配下のディレクトリモジュール (main.rs) を自動認識しないため明示する。
[[test]]
name = "prop_font"
path = "tests/prop_font/main.rs"
